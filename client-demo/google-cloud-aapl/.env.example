# Safe default: deterministic, no network, no paid calls.
AAPL_DEMO_MODE=fixture
AAPL_DEMO_SYNTHESIS=deterministic
GEMINI_MODEL=gemini-2.5-flash
DELTASIGNAL_MCP_ENDPOINT=https://api.aitrailblazer.net/mcp

# Live Cloud Run only. Supply this from Secret Manager; never commit a value.
# DELTASIGNAL_MCP_API_KEY=
